import { Artwork } from './types';

// sampleArtworks is no longer used as the primary source of data.
// Artworks are fetched from the backend in App.tsx.
// This can be kept empty or removed if no other part of the app refers to it.
export const sampleArtworks: Artwork[] = [];